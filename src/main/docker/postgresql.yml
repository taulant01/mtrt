version: '2'
services:
  mtrt-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/mtrt/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=mtrt
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
